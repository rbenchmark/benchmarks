LEVEL = ../../../tests

#if use R, no need PARA
PARA=<revcomp-input.txt
PROG=reverse
CFLAG=-pipe -Wall -O3 -fomit-frame-pointer -march=native -std=c99 -pthread
JFLAG= -server -XX:+TieredCompilation -XX:+AggressiveOpts -Xint
include $(LEVEL)/Makefile.common


PYTHON = python3
