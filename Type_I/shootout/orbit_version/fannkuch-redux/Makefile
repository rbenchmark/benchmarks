LEVEL = ../../..

PARA=8
PROG=fannkuch-redux
CFLAG=-pipe -Wall -O3 -fomit-frame-pointer -march=native
JFLAG=-server -XX:+TieredCompilation -XX:+AggressiveOpts

include $(LEVEL)/common.mk
REP=2


